<Window x:Class="SirThaddeus.DesktopRuntime.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:conv="clr-namespace:SirThaddeus.DesktopRuntime.Converters"
        xmlns:views="clr-namespace:SirThaddeus.DesktopRuntime.Views"
        xmlns:controls="clr-namespace:SirThaddeus.DesktopRuntime.Controls"
        Title="Sir Thaddeus"
        Width="960"
        Height="640"
        MinWidth="640"
        MinHeight="400"
        WindowStartupLocation="CenterScreen"
        WindowStyle="SingleBorderWindow"
        ResizeMode="CanResize"
        Background="{DynamicResource BaseBrush}"
        Foreground="{DynamicResource TextBrush}"
        KeyDown="Window_KeyDown">

    <Window.Resources>
        <!-- Converters (window-specific; palette + styles come from App-level theme) -->
        <BooleanToVisibilityConverter x:Key="BoolToVis"/>
        <conv:Base64ToImageConverter  x:Key="Base64ToImage"/>
        <conv:StringToBrushConverter  x:Key="StringToBrush"/>
        <conv:UrlToImageConverter     x:Key="UrlToImage"/>
        <conv:MarkdownToFlowDocumentConverter x:Key="MarkdownToFlowDocument"/>
        <conv:StringMatchConverter x:Key="StringMatchConverter"/>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Header -->
            <RowDefinition Height="*"/>     <!-- Content (split) -->
            <RowDefinition Height="Auto"/>  <!-- Input -->
        </Grid.RowDefinitions>

        <!-- ═══════════════════════════════════════════════════════════
             HEADER
             ═══════════════════════════════════════════════════════ -->
        <Border Grid.Row="0"
                Background="{StaticResource MantleBrush}"
                Padding="16,10"
                BorderBrush="{StaticResource Surface0Brush}"
                BorderThickness="0,0,0,1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto"/> <!-- Status -->
                    <ColumnDefinition Width="*"/>    <!-- Tabs (Center) -->
                    <ColumnDefinition Width="Auto"/> <!-- STOP ALL -->
                </Grid.ColumnDefinitions>

                <!-- ── 1. Session Selector + Status (Left) ── -->
                <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">

                    <!-- Chat Session Dropdown (text-first, no emoji) -->
                    <Button x:Name="ChatHistoryToggle"
                            Click="ChatHistoryToggle_Click"
                            VerticalAlignment="Center"
                            Cursor="Hand"
                            Margin="0,0,14,0"
                            ToolTip="Toggle chat history">
                        <Button.Template>
                            <ControlTemplate TargetType="Button">
                                <Border x:Name="DropdownBg"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        BorderThickness="1"
                                        CornerRadius="4"
                                        Padding="14,6">
                                    <StackPanel Orientation="Horizontal">
                                        <TextBlock Text="Conversation"
                                                   FontSize="13"
                                                   FontWeight="SemiBold"
                                                   Foreground="{StaticResource TextBrush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Text="▾"
                                                   FontSize="13"
                                                   Foreground="{StaticResource Subtext1Brush}"
                                                   VerticalAlignment="Center"
                                                   Margin="6,0,0,0"/>
                                    </StackPanel>
                                </Border>
                                <ControlTemplate.Triggers>
                                    <Trigger Property="IsMouseOver" Value="True">
                                        <Setter TargetName="DropdownBg" Property="Background" Value="{StaticResource Surface0Brush}"/>
                                        <Setter TargetName="DropdownBg" Property="BorderBrush" Value="{StaticResource Surface1Brush}"/>
                                    </Trigger>
                                    <Trigger Property="IsPressed" Value="True">
                                        <Setter TargetName="DropdownBg" Property="Background" Value="{StaticResource Surface1Brush}"/>
                                        <Setter TargetName="DropdownBg" Property="BorderBrush" Value="{StaticResource Surface2Brush}"/>
                                    </Trigger>
                                </ControlTemplate.Triggers>
                            </ControlTemplate>
                        </Button.Template>
                    </Button>


                    <!-- Status Indicator — text-only, semantic color by state -->
                    <Border x:Name="HeaderArea"
                            Background="Transparent"
                            Padding="4,2"
                            Cursor="Hand"
                            ToolTip="Click to view Activity Drawer">
                        <Border.InputBindings>
                            <MouseBinding Gesture="LeftClick" Command="{Binding ToggleDrawerCommand}" />
                        </Border.InputBindings>
                        <TextBlock Text="{Binding StateLabel}"
                                   FontSize="13"
                                   FontWeight="Medium"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <!-- Default: normal muted text (Connected / idle) -->
                                    <Setter Property="Foreground" Value="{StaticResource Subtext0Brush}"/>
                                    <Style.Triggers>
                                        <!-- Listening → muted blue -->
                                        <DataTrigger Binding="{Binding StateLabel}" Value="Listening">
                                            <Setter Property="Foreground" Value="{StaticResource BlueBrush}"/>
                                        </DataTrigger>
                                        <!-- Thinking → muted blue -->
                                        <DataTrigger Binding="{Binding StateLabel}" Value="Thinking">
                                            <Setter Property="Foreground" Value="{StaticResource BlueBrush}"/>
                                        </DataTrigger>
                                        <!-- Speaking → mauve -->
                                        <DataTrigger Binding="{Binding StateLabel}" Value="Speaking">
                                            <Setter Property="Foreground" Value="{StaticResource MauveBrush}"/>
                                        </DataTrigger>
                                        <!-- Working in background → amber -->
                                        <DataTrigger Binding="{Binding StateLabel}" Value="Working in background">
                                            <Setter Property="Foreground" Value="{StaticResource YellowBrush}"/>
                                        </DataTrigger>
                                        <!-- Stopped → red -->
                                        <DataTrigger Binding="{Binding StateLabel}" Value="Stopped">
                                            <Setter Property="Foreground" Value="{StaticResource RedBrush}"/>
                                        </DataTrigger>
                                        <!-- Error → red -->
                                        <DataTrigger Binding="{Binding StateLabel}" Value="Error">
                                            <Setter Property="Foreground" Value="{StaticResource RedBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </Border>
                </StackPanel>

                <!-- ── 2. View Toggle Tabs (Center) ── -->
                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center">
                    <ToggleButton x:Name="ChatTabButton"
                                  Content="Chat"
                                  IsChecked="True"
                                  Click="ChatTab_Click"
                                  ToolTip="Chat (Ctrl+1)"
                                  AutomationProperties.Name="Chat tab"
                                  Style="{StaticResource ViewTabStyle}"/>
                    <ToggleButton x:Name="BriefingTabButton"
                                  Content="Briefing"
                                  IsChecked="False"
                                  Click="BriefingTab_Click"
                                  ToolTip="Deep-Dive Briefings (Ctrl+2)"
                                  AutomationProperties.Name="Briefing tab"
                                  Style="{StaticResource ViewTabStyle}"/>
                    <ToggleButton x:Name="SettingsTabButton"
                                  Content="Settings"
                                  IsChecked="False"
                                  Click="SettingsTab_Click"
                                  ToolTip="Settings (Ctrl+6)"
                                  AutomationProperties.Name="Settings tab"
                                  Style="{StaticResource ViewTabStyle}"/>
                </StackPanel>

                <!-- ── 3. Controls (Right) ── -->
                <StackPanel Grid.Column="2" Orientation="Horizontal" HorizontalAlignment="Right" VerticalAlignment="Center">
                    <!-- New Chat (only visible in Chat mode) -->
                    <Button x:Name="NewChatButton"
                            Content="New Chat"
                            Command="{Binding ClearCommand}"
                            Style="{StaticResource HeaderButtonStyle}"
                            Margin="0,0,16,0"/>

                    <StackPanel DataContext="{Binding ElementName=HeaderArea, Path=DataContext}">
                        <Button Content="⏹ STOP ALL"
                                Command="{Binding StopAllCommand}"
                                Style="{StaticResource CancelButtonStyle}"
                                Padding="12,6"
                                FontSize="12"
                                Margin="0,0,0,0"/>
                    </StackPanel>
                </StackPanel>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════
             CONTENT — SPLIT PANE (Chat | Activity Log)
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="ChatView">
            <Grid.ColumnDefinitions>
                <ColumnDefinition x:Name="ChatHistoryColumn" Width="0"/>
                <ColumnDefinition Width="*" MinWidth="260"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <!-- ── Chat History Sidebar (collapsible) ─────────────── -->
            <Border Grid.Column="0"
                    x:Name="ChatHistoryPanel"
                    Background="{StaticResource MantleBrush}"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,0,1,0"
                    Visibility="Collapsed">
                <DockPanel>
                    <Border DockPanel.Dock="Top"
                            Padding="10,10"
                            BorderBrush="{StaticResource Surface0Brush}"
                            BorderThickness="0,0,0,1">
                        <Grid>
                            <TextBlock Text="Chat History"
                                       FontSize="13"
                                       FontWeight="SemiBold"
                                       Foreground="{StaticResource TextBrush}"
                                       VerticalAlignment="Center" />
                            <Button HorizontalAlignment="Right"
                                    Content="New Chat"
                                    Command="{Binding ClearCommand}"
                                    Style="{StaticResource HeaderButtonStyle}"
                                    FontSize="11"
                                    ToolTip="Create a new chat" />
                        </Grid>
                    </Border>
                    <ScrollViewer VerticalScrollBarVisibility="Auto"
                                  HorizontalScrollBarVisibility="Disabled">
                        <ItemsControl ItemsSource="{Binding ChatHistory}" Padding="6">
                                <ItemsControl.ItemTemplate>
                                    <DataTemplate>
                                        <Border Margin="0,0,0,4"
                                                Padding="8,6"
                                                CornerRadius="6"
                                                Background="{StaticResource Surface0Brush}"
                                                BorderBrush="{StaticResource Surface1Brush}"
                                                BorderThickness="1"
                                                Cursor="Hand"
                                                MouseLeftButtonUp="ChatHistoryItem_Click">
                                            <Grid>
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="*" />
                                                    <ColumnDefinition Width="Auto" />
                                                </Grid.ColumnDefinitions>
                                                <StackPanel Grid.Column="0">
                                                    <TextBlock Text="{Binding Title}"
                                                               FontSize="11"
                                                               FontWeight="SemiBold"
                                                               Foreground="{StaticResource TextBrush}"
                                                               TextTrimming="CharacterEllipsis"
                                                               MaxWidth="160" />
                                                    <StackPanel Orientation="Horizontal" Margin="0,2,0,0">
                                                        <TextBlock Text="{Binding MessageCount, StringFormat={}{0} msgs}"
                                                                   FontSize="9"
                                                                   Foreground="{StaticResource Subtext0Brush}" />
                                                        <TextBlock Text=" · "
                                                                   FontSize="9"
                                                                   Foreground="{StaticResource Overlay0Brush}" />
                                                        <TextBlock Text="{Binding TimestampDisplay}"
                                                                   FontSize="9"
                                                                   Foreground="{StaticResource Overlay0Brush}" />
                                                    </StackPanel>
                                                </StackPanel>
                                                <Button Grid.Column="1"
                                                        Content="✕"
                                                        VerticalAlignment="Top"
                                                        HorizontalAlignment="Right"
                                                        Command="{Binding DataContext.DeleteChatSessionCommand, RelativeSource={RelativeSource AncestorType={x:Type ItemsControl}}}"
                                                        CommandParameter="{Binding}"
                                                        Background="Transparent"
                                                        BorderThickness="0"
                                                        Foreground="{StaticResource Overlay0Brush}"
                                                        FontSize="10"
                                                        Padding="4,2"
                                                        Cursor="Hand"
                                                        ToolTip="Delete this chat" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ItemsControl.ItemTemplate>
                            </ItemsControl>
                    </ScrollViewer>
                </DockPanel>
            </Border>

            <!-- (ChatHistoryToggle moved to header bar) -->


            <!-- ── Left: Chat Messages ─────────────────────────────── -->
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- ── Voice Warmup Banner ── -->
                <Border Grid.Row="0"
                        Margin="10,10,10,0"
                        Padding="12,8"
                        CornerRadius="6"
                        Background="#2D89DCEB"
                        BorderBrush="{StaticResource BlueBrush}"
                        BorderThickness="1">
                    <Border.Style>
                        <Style TargetType="Border">
                            <!-- Default to Visible (showing while warming up) -->
                            <Setter Property="Visibility" Value="Visible"/>
                            <Style.Triggers>
                                <!-- Hide once ready -->
                                <DataTrigger Binding="{Binding IsVoiceReady}" Value="True">
                                    <Setter Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </Border.Style>
                    <StackPanel Orientation="Horizontal">
                        <TextBlock Text="&#x23F3;" FontSize="16" Foreground="{StaticResource BlueBrush}" Margin="0,0,8,0" VerticalAlignment="Center"/>
                        <TextBlock Text="{Binding VoiceWarmupStatus}" 
                                   Foreground="{StaticResource TextBrush}" 
                                   FontSize="13" 
                                   VerticalAlignment="Center"/>
                    </StackPanel>
                </Border>

                <ScrollViewer Grid.Row="1"
                              x:Name="ChatScroller"
                              Background="{StaticResource BaseBrush}"
                              VerticalScrollBarVisibility="Auto"
                              HorizontalScrollBarVisibility="Disabled">
                    <Grid>
                    <!-- Empty State -->
                    <StackPanel HorizontalAlignment="Center"
                                VerticalAlignment="Center"
                                Margin="0,100,0,0">
                        <StackPanel.Style>
                            <Style TargetType="StackPanel">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding Messages.Count}" Value="0">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </StackPanel.Style>
                        <TextBlock Text="Your AI. Your Rules."
                                   FontSize="28"
                                   FontFamily="{StaticResource PrimaryFont}"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource TextBrush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,8"/>
                        <TextBlock Text="A local-first copilot that only acts with explicit permission."
                                   FontSize="14"
                                   FontFamily="{StaticResource PrimaryFont}"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,24"/>
                        <TextBlock Text="Hold to talk (Ctrl+Alt+M)"
                                   FontSize="13"
                                   FontFamily="{StaticResource PrimaryFont}"
                                   Foreground="{StaticResource Overlay0Brush}"
                                   HorizontalAlignment="Center"
                                   Margin="0,0,0,4"/>
                        <TextBlock Text="Command Palette (Ctrl+Space)"
                                   FontSize="13"
                                   FontFamily="{StaticResource PrimaryFont}"
                                   Foreground="{StaticResource Overlay0Brush}"
                                   HorizontalAlignment="Center"/>
                    </StackPanel>

                    <ItemsControl ItemsSource="{Binding Messages}"
                                  Padding="16,12">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <!-- ═══════════════════════════════════
                                 LEDGER ENTRY
                                 3-column grid: Spine | Spacer | Content
                                 IsMouseOver on the outer Grid drives
                                 timestamp + overflow reveal.
                                 ═══════════════════════════════ -->
                            <Grid x:Name="LedgerEntry" Margin="0,0,0,16"
                                  Opacity="0"
                                  RenderTransformOrigin="0,0">
                                <Grid.RenderTransform>
                                    <TranslateTransform/>
                                </Grid.RenderTransform>
                                <Grid.Triggers>
                                    <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                 From="0" To="1" Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                                <DoubleAnimation Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.Y)"
                                                                 From="8" To="0" Duration="0:0:0.2">
                                                    <DoubleAnimation.EasingFunction>
                                                        <CubicEase EasingMode="EaseOut"/>
                                                    </DoubleAnimation.EasingFunction>
                                                </DoubleAnimation>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </EventTrigger>
                                </Grid.Triggers>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="3"/>
                                    <ColumnDefinition Width="10"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <!-- ── Left Spine ────────────────── -->
                                <Border x:Name="Spine"
                                        Grid.Column="0"
                                        Background="{StaticResource SpineResultBrush}"
                                        CornerRadius="0"
                                        VerticalAlignment="Stretch"/>

                                <!-- ── Content Column ────────────── -->
                                <StackPanel Grid.Column="2">

                                    <!-- ── Header row (author + role + timestamp + overflow) ── -->
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Author + Role label -->
                                        <StackPanel Grid.Column="0" Orientation="Horizontal">
                                            <TextBlock Text="{Binding AuthorLabel}"
                                                       FontSize="11"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource Subtext1Brush}"/>
                                            <TextBlock Text=" — "
                                                       FontSize="11"
                                                       Foreground="{StaticResource Overlay0Brush}"/>
                                            <TextBlock x:Name="RoleTag"
                                                       Text="{Binding RoleLabel}"
                                                       FontSize="10"
                                                       FontWeight="SemiBold"
                                                       Foreground="{StaticResource Overlay0Brush}"
                                                       VerticalAlignment="Center"/>
                                        </StackPanel>

                                        <!-- Timestamp (reveal on hover) -->
                                        <TextBlock x:Name="TimeLabel"
                                                   Grid.Column="1"
                                                   Text="{Binding TimeDisplay}"
                                                   FontSize="10"
                                                   Foreground="{StaticResource Overlay0Brush}"
                                                   VerticalAlignment="Center"
                                                   Margin="8,0,0,0"
                                                   Opacity="0"
                                                   IsHitTestVisible="False"/>

                                        <!-- Overflow menu button (reveal on hover) -->
                                        <Button x:Name="OverflowBtn"
                                                Grid.Column="2"
                                                Content="⋮"
                                                FontSize="14"
                                                Foreground="{StaticResource Overlay0Brush}"
                                                Background="Transparent"
                                                BorderThickness="0"
                                                Cursor="Hand"
                                                MinWidth="24"
                                                MinHeight="24"
                                                Padding="6,4"
                                                Margin="6,0,8,0"
                                                VerticalAlignment="Center"
                                                Opacity="0"
                                                Click="OverflowBtn_Click"
                                                Visibility="Collapsed">
                                            <Button.ContextMenu>
                                                <ContextMenu>
                                                    <MenuItem Header="Copy" Click="CopyMessage_Click"/>
                                                    <MenuItem Header="Retry" Click="RetryMessage_Click"/>
                                                    <MenuItem Header="Read Aloud" Click="ReadAloudMessage_Click"/>
                                                </ContextMenu>
                                            </Button.ContextMenu>
                                        </Button>
                                    </Grid>

                                    <!-- ── Thinking expander ── -->
                                    <Border x:Name="ThinkingPanel"
                                            Visibility="Collapsed"
                                            Margin="0,0,0,8"
                                            Background="{StaticResource ToolActivityBgBrush}"
                                            BorderBrush="{StaticResource Surface1Brush}"
                                            BorderThickness="1"
                                            CornerRadius="4"
                                            Padding="8,6">
                                        <Expander IsExpanded="{Binding IsThoughtExpanded, Mode=TwoWay}"
                                                  Background="Transparent"
                                                  BorderThickness="0"
                                                  Foreground="{StaticResource Subtext0Brush}">
                                            <Expander.Header>
                                                <TextBlock Text="Model reasoning trace"
                                                           FontSize="11"
                                                           FontWeight="SemiBold"
                                                           Foreground="{StaticResource Subtext0Brush}"/>
                                            </Expander.Header>
                                            <TextBox Text="{Binding ThoughtContent, Mode=OneWay}"
                                                     Margin="0,6,0,0"
                                                     TextWrapping="Wrap"
                                                     FontSize="12"
                                                     FontFamily="Consolas, Cascadia Mono, monospace"
                                                     Foreground="{StaticResource Overlay0Brush}"
                                                     Background="Transparent"
                                                     BorderThickness="0"
                                                     Padding="0"
                                                     IsReadOnly="True"
                                                     IsReadOnlyCaretVisible="False"
                                                     IsTabStop="False"
                                                     FocusVisualStyle="{x:Null}"
                                                     Cursor="IBeam"
                                                     SelectionBrush="{StaticResource BlueBrush}"
                                                     SelectionOpacity="0.35"
                                                     CaretBrush="{StaticResource TextBrush}"
                                                     PreviewMouseWheel="BubbleMouseWheel"/>
                                        </Expander>
                                    </Border>

                                    <!-- ── Message body (Markdown) ── -->
                                    <FlowDocumentScrollViewer x:Name="ContentMarkdown"
                                                             Document="{Binding Content, Converter={StaticResource MarkdownToFlowDocument}}"
                                                             FontSize="13"
                                                             FontFamily="Segoe UI"
                                                             Foreground="{StaticResource TextBrush}"
                                                             Background="Transparent"
                                                             FocusVisualStyle="{x:Null}"
                                                             IsTabStop="False"
                                                             IsSelectionEnabled="True"
                                                             VerticalScrollBarVisibility="Hidden"
                                                             HorizontalScrollBarVisibility="Hidden"
                                                             PreviewMouseWheel="BubbleMouseWheel"/>

                                    <!-- ── Message body (Plain text, tool/status) ── -->
                                    <TextBox x:Name="ContentPlain"
                                             Text="{Binding Content, Mode=OneWay}"
                                             Visibility="Collapsed"
                                             TextWrapping="Wrap"
                                             FontSize="13"
                                             FontFamily="Segoe UI"
                                             Foreground="{StaticResource TextBrush}"
                                             Background="Transparent"
                                             BorderThickness="0"
                                             Padding="0"
                                             IsReadOnly="True"
                                             IsReadOnlyCaretVisible="False"
                                             IsTabStop="False"
                                             FocusVisualStyle="{x:Null}"
                                             Cursor="IBeam"
                                             SelectionBrush="{StaticResource BlueBrush}"
                                             SelectionOpacity="0.35"
                                             CaretBrush="{StaticResource TextBrush}"
                                             PreviewMouseWheel="BubbleMouseWheel"/>

                                    <!-- (Legacy actions removed, now in ContextMenu) -->

                                    <!-- ══════════════════════════════════════
                                         SOURCE CARDS CAROUSEL
                                         ══════════════════════════════════ -->
                                    <Grid x:Name="SourceCardsPanel"
                                          Visibility="Collapsed"
                                          Margin="0,12,0,4">
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>

                                        <!-- Left arrow -->
                                        <Button Grid.Row="0"
                                                Command="{Binding PrevPageCommand}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Left"
                                                Panel.ZIndex="10"
                                                Cursor="Hand"
                                                Width="30" Height="30"
                                                Margin="-4,0,0,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CanGoLeft}" Value="False">
                                                            <Setter Property="Visibility" Value="Hidden"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="ArrowBg"
                                                            Background="#DD1E1E2E"
                                                            CornerRadius="15"
                                                            Width="30" Height="30">
                                                        <TextBlock Text="&#x276E;"
                                                                   FontSize="14"
                                                                   Foreground="{StaticResource TextBrush}"
                                                                   HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ArrowBg"
                                                                    Property="Background" Value="#EE45475A"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <!-- Right arrow -->
                                        <Button Grid.Row="0"
                                                Command="{Binding NextPageCommand}"
                                                VerticalAlignment="Center"
                                                HorizontalAlignment="Right"
                                                Panel.ZIndex="10"
                                                Cursor="Hand"
                                                Width="30" Height="30"
                                                Margin="0,0,-4,0">
                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="Visibility" Value="Visible"/>
                                                    <Style.Triggers>
                                                        <DataTrigger Binding="{Binding CanGoRight}" Value="False">
                                                            <Setter Property="Visibility" Value="Hidden"/>
                                                        </DataTrigger>
                                                    </Style.Triggers>
                                                </Style>
                                            </Button.Style>
                                            <Button.Template>
                                                <ControlTemplate TargetType="Button">
                                                    <Border x:Name="ArrowBg"
                                                            Background="#DD1E1E2E"
                                                            CornerRadius="15"
                                                            Width="30" Height="30">
                                                        <TextBlock Text="&#x276F;"
                                                                   FontSize="14"
                                                                   Foreground="{StaticResource TextBrush}"
                                                                   HorizontalAlignment="Center"
                                                                   VerticalAlignment="Center"/>
                                                    </Border>
                                                    <ControlTemplate.Triggers>
                                                        <Trigger Property="IsMouseOver" Value="True">
                                                            <Setter TargetName="ArrowBg"
                                                                    Property="Background" Value="#EE45475A"/>
                                                        </Trigger>
                                                    </ControlTemplate.Triggers>
                                                </ControlTemplate>
                                            </Button.Template>
                                        </Button>

                                        <!-- Card row -->
                                        <ItemsControl Grid.Row="0"
                                                      ItemsSource="{Binding VisibleCards}"
                                                      Margin="10,0,10,0">
                                            <ItemsControl.ItemsPanel>
                                                <ItemsPanelTemplate>
                                                    <StackPanel Orientation="Horizontal"/>
                                                </ItemsPanelTemplate>
                                            </ItemsControl.ItemsPanel>
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate>
                                                    <Button Click="SourceCard_Click"
                                                            Cursor="Hand"
                                                            Margin="0,0,10,0"
                                                            VerticalAlignment="Top"
                                                            Opacity="0"
                                                            RenderTransformOrigin="0,0.5">
                                                        <Button.RenderTransform>
                                                            <TranslateTransform/>
                                                        </Button.RenderTransform>
                                                        <Button.Triggers>
                                                            <EventTrigger RoutedEvent="FrameworkElement.Loaded">
                                                                <BeginStoryboard>
                                                                    <Storyboard>
                                                                        <DoubleAnimation
                                                                            Storyboard.TargetProperty="Opacity"
                                                                            From="0" To="1"
                                                                            Duration="0:0:0.15"/>
                                                                        <DoubleAnimation
                                                                            Storyboard.TargetProperty="(UIElement.RenderTransform).(TranslateTransform.X)"
                                                                            From="30" To="0"
                                                                            Duration="0:0:0.15">
                                                                            <DoubleAnimation.EasingFunction>
                                                                                <CubicEase EasingMode="EaseOut"/>
                                                                            </DoubleAnimation.EasingFunction>
                                                                        </DoubleAnimation>
                                                                    </Storyboard>
                                                                </BeginStoryboard>
                                                            </EventTrigger>
                                                        </Button.Triggers>
                                                        <Button.Template>
                                                            <ControlTemplate TargetType="Button">
                                                                <Border x:Name="CardBorder"
                                                                        Background="{StaticResource Surface1Brush}"
                                                                        CornerRadius="4"
                                                                        Width="198"
                                                                        ClipToBounds="True"
                                                                        SnapsToDevicePixels="True">
                                                                    <StackPanel>
                                                                        <Grid Height="110"
                                                                              Background="{StaticResource Surface0Brush}">
                                                                            <Image x:Name="ThumbnailImg"
                                                                                   Source="{Binding ThumbnailUrl,
                                                                                            Converter={StaticResource UrlToImage}}"
                                                                                   Stretch="UniformToFill"
                                                                                   HorizontalAlignment="Center"
                                                                                   VerticalAlignment="Center"
                                                                                   Visibility="Collapsed"/>
                                                                            <Border x:Name="ThumbPlaceholder"
                                                                                    Background="{Binding AvatarColor,
                                                                                                 Converter={StaticResource StringToBrush}}">
                                                                                <TextBlock Text="{Binding AvatarLetter}"
                                                                                           FontSize="32"
                                                                                           FontWeight="Bold"
                                                                                           Foreground="{DynamicResource CrustBrush}"
                                                                                           Opacity="0.35"
                                                                                           HorizontalAlignment="Center"
                                                                                           VerticalAlignment="Center"/>
                                                                            </Border>
                                                                        </Grid>
                                                                        <StackPanel Margin="12,8,12,10">
                                                                            <Grid Height="18" Margin="0,0,0,5">
                                                                                <Grid.ColumnDefinitions>
                                                                                    <ColumnDefinition Width="18"/>
                                                                                    <ColumnDefinition Width="*"/>
                                                                                </Grid.ColumnDefinitions>
                                                                                <Image x:Name="FaviconImg"
                                                                                       Grid.Column="0"
                                                                                       Source="{Binding FaviconBase64,
                                                                                                Converter={StaticResource Base64ToImage}}"
                                                                                       Width="14" Height="14"
                                                                                       HorizontalAlignment="Center"
                                                                                       VerticalAlignment="Center"
                                                                                       Visibility="Collapsed"/>
                                                                                <Border x:Name="AvatarCircle"
                                                                                        Grid.Column="0"
                                                                                        Width="16" Height="16"
                                                                                        CornerRadius="8"
                                                                                        HorizontalAlignment="Center"
                                                                                        VerticalAlignment="Center"
                                                                                        Background="{Binding AvatarColor,
                                                                                                     Converter={StaticResource StringToBrush}}">
                                                                                    <TextBlock Text="{Binding AvatarLetter}"
                                                                                               FontSize="8"
                                                                                               FontWeight="Bold"
                                                                                               Foreground="{DynamicResource CrustBrush}"
                                                                                               HorizontalAlignment="Center"
                                                                                               VerticalAlignment="Center"/>
                                                                                </Border>
                                                                                <TextBlock Grid.Column="1"
                                                                                           Text="{Binding Domain}"
                                                                                           FontSize="10.5"
                                                                                           Foreground="{StaticResource Overlay0Brush}"
                                                                                           VerticalAlignment="Center"
                                                                                           Margin="5,0,0,0"
                                                                                           TextTrimming="CharacterEllipsis"/>
                                                                            </Grid>
                                                                            <TextBlock Text="{Binding Title}"
                                                                                       FontSize="12.5"
                                                                                       FontWeight="SemiBold"
                                                                                       Foreground="{StaticResource TextBrush}"
                                                                                       TextWrapping="Wrap"
                                                                                       MaxHeight="36"
                                                                                       TextTrimming="CharacterEllipsis"
                                                                                       Margin="0,0,0,4"/>
                                                                            <TextBlock Text="{Binding Excerpt}"
                                                                                       FontSize="11"
                                                                                       Foreground="{StaticResource Subtext0Brush}"
                                                                                       TextWrapping="Wrap"
                                                                                       MaxHeight="64"
                                                                                       TextTrimming="CharacterEllipsis"
                                                                                       LineHeight="15"/>
                                                                        </StackPanel>
                                                                    </StackPanel>
                                                                </Border>
                                                                <ControlTemplate.Triggers>
                                                                    <Trigger Property="IsMouseOver" Value="True">
                                                                        <Setter TargetName="CardBorder"
                                                                                Property="Background"
                                                                                Value="{StaticResource Surface2Brush}"/>
                                                                    </Trigger>
                                                                    <DataTrigger Binding="{Binding HasThumbnail}" Value="True">
                                                                        <Setter TargetName="ThumbnailImg"
                                                                                Property="Visibility" Value="Visible"/>
                                                                        <Setter TargetName="ThumbPlaceholder"
                                                                                Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                    <DataTrigger Binding="{Binding HasFavicon}" Value="True">
                                                                        <Setter TargetName="FaviconImg"
                                                                                Property="Visibility" Value="Visible"/>
                                                                        <Setter TargetName="AvatarCircle"
                                                                                Property="Visibility" Value="Collapsed"/>
                                                                    </DataTrigger>
                                                                </ControlTemplate.Triggers>
                                                            </ControlTemplate>
                                                        </Button.Template>
                                                    </Button>
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>

                                        <!-- Page indicator -->
                                        <TextBlock Grid.Row="1"
                                                   Text="{Binding CarouselPageLabel}"
                                                   FontSize="11"
                                                   Foreground="{StaticResource Overlay0Brush}"
                                                   HorizontalAlignment="Center"
                                                   Margin="0,6,0,0"/>
                                    </Grid>

                                </StackPanel>
                            </Grid>

                            <DataTemplate.Triggers>
                                <!-- ── Hover: reveal timestamp + overflow (delayed fade-out) ── -->
                                <Trigger SourceName="LedgerEntry" Property="IsMouseOver" Value="True">
                                    <Trigger.EnterActions>
                                        <BeginStoryboard x:Name="ShowHoverElements">
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="TimeLabel"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="1" Duration="0:0:0.08"/>
                                                <DoubleAnimation Storyboard.TargetName="OverflowBtn"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="1" Duration="0:0:0.08"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.EnterActions>
                                    <Trigger.ExitActions>
                                        <StopStoryboard BeginStoryboardName="ShowHoverElements"/>
                                        <BeginStoryboard>
                                            <Storyboard>
                                                <DoubleAnimation Storyboard.TargetName="TimeLabel"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="0" BeginTime="0:0:0.45" Duration="0:0:0.2"/>
                                                <DoubleAnimation Storyboard.TargetName="OverflowBtn"
                                                                 Storyboard.TargetProperty="Opacity"
                                                                 To="0" BeginTime="0:0:0.45" Duration="0:0:0.2"/>
                                            </Storyboard>
                                        </BeginStoryboard>
                                    </Trigger.ExitActions>
                                </Trigger>

                                <!-- ── Source cards when present ── -->
                                <DataTrigger Binding="{Binding HasSourceCards}" Value="True">
                                    <Setter TargetName="SourceCardsPanel"
                                            Property="Visibility" Value="Visible"/>
                                </DataTrigger>

                                <!-- ── Thought traces ── -->
                                <DataTrigger Binding="{Binding HasThoughtContent}" Value="True">
                                    <Setter TargetName="ThinkingPanel"
                                            Property="Visibility" Value="Visible"/>
                                </DataTrigger>

                                <!-- ── Assistant: result spine + overflow visible ── -->
                                <DataTrigger Binding="{Binding IsAssistant}" Value="True">
                                    <Setter TargetName="Spine" Property="Background" Value="{StaticResource SpineResultBrush}"/>
                                    <Setter TargetName="OverflowBtn" Property="Visibility" Value="Visible"/>
                                </DataTrigger>

                                <!-- ── User: command spine ── -->
                                <DataTrigger Binding="{Binding IsUser}" Value="True">
                                    <Setter TargetName="Spine" Property="Background" Value="{StaticResource SpineCommandBrush}"/>
                                    <Setter TargetName="OverflowBtn" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="SourceCardsPanel" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ThinkingPanel" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>

                                <!-- ── Tool activity: indented, pill bg ── -->
                                <DataTrigger Binding="{Binding IsToolActivity}" Value="True">
                                    <Setter TargetName="Spine" Property="Background" Value="{StaticResource SpineToolBrush}"/>
                                    <Setter TargetName="LedgerEntry" Property="Margin" Value="16,0,0,12"/>
                                    <Setter TargetName="ContentMarkdown" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ContentPlain" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ContentPlain" Property="FontSize" Value="12"/>
                                    <Setter TargetName="ContentPlain" Property="Foreground" Value="{StaticResource Overlay0Brush}"/>
                                    <Setter TargetName="ContentPlain" Property="Background" Value="{StaticResource ToolActivityBgBrush}"/>
                                    <Setter TargetName="ContentPlain" Property="Padding" Value="8,6"/>
                                    <Setter TargetName="OverflowBtn" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="SourceCardsPanel" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ThinkingPanel" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>

                                <!-- ── Status: minimal ── -->
                                <DataTrigger Binding="{Binding IsStatus}" Value="True">
                                    <Setter TargetName="Spine" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ContentMarkdown" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ContentPlain" Property="Visibility" Value="Visible"/>
                                    <Setter TargetName="ContentPlain" Property="FontStyle" Value="Italic"/>
                                    <Setter TargetName="ContentPlain" Property="FontSize" Value="12"/>
                                    <Setter TargetName="ContentPlain" Property="Foreground" Value="{StaticResource Overlay0Brush}"/>
                                    <Setter TargetName="OverflowBtn" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="SourceCardsPanel" Property="Visibility" Value="Collapsed"/>
                                    <Setter TargetName="ThinkingPanel" Property="Visibility" Value="Collapsed"/>
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
                </Grid>
            </ScrollViewer>
            </Grid>

        </Grid>


        <!-- ═══════════════════════════════════════════════════════════
             MEMORY BROWSER
             User-initiated data management panel. DataContext is set
             to MemoryBrowserViewModel in code-behind.
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="MemoryView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Sub-tabs + search -->
                <RowDefinition Height="*"/>     <!-- DataGrid area -->
                <RowDefinition Height="Auto"/>  <!-- Footer: pagination + actions -->
            </Grid.RowDefinitions>

            <!-- ── Sub-tabs + Search Bar ─────────────────────────────── -->
            <Border Grid.Row="0"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,8"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Sub-tabs -->
                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <RadioButton Content="Facts"
                                     IsChecked="True"
                                     GroupName="MemoryTabs"
                                     Click="MemorySubTab_Click"
                                     Tag="Facts"
                                     Style="{StaticResource MemorySubTabStyle}"/>
                        <RadioButton Content="Events"
                                     GroupName="MemoryTabs"
                                     Click="MemorySubTab_Click"
                                     Tag="Events"
                                     Style="{StaticResource MemorySubTabStyle}"
                                     Margin="4,0,0,0"/>
                        <RadioButton Content="Chunks"
                                     GroupName="MemoryTabs"
                                     Click="MemorySubTab_Click"
                                     Tag="Chunks"
                                     Style="{StaticResource MemorySubTabStyle}"
                                     Margin="4,0,0,0"/>
                    </StackPanel>

                    <!-- Search box -->
                    <TextBox Grid.Column="1"
                             x:Name="MemorySearchBox"
                             Text="{Binding SearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ChatInputStyle}"
                             FontSize="12"
                             Padding="8,5"
                             Margin="16,0,0,0"
                             Tag="Search memories..."/>
                </Grid>
            </Border>

            <!-- ── DataGrid Area ─────────────────────────────────────── -->
            <Grid Grid.Row="1" Background="{StaticResource BaseBrush}">

                <!-- Facts Grid -->
                <DataGrid x:Name="FactsGrid"
                          ItemsSource="{Binding Facts}"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          Style="{StaticResource MemoryDataGridStyle}"
                          CellStyle="{StaticResource MemoryDataGridCellStyle}"
                          ColumnHeaderStyle="{StaticResource MemoryColumnHeaderStyle}"
                          RowStyle="{StaticResource MemoryDataGridRowStyle}"
                          CellEditEnding="DataGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Profile"    Binding="{Binding ProfileId}"  Width="90" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Subject"    Binding="{Binding Subject}"   Width="*" />
                        <DataGridTextColumn Header="Predicate"  Binding="{Binding Predicate}" Width="*" />
                        <DataGridTextColumn Header="Object"     Binding="{Binding Object}"    Width="1.5*" />
                        <DataGridTextColumn Header="Confidence" Binding="{Binding Confidence, StringFormat=F2}" Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Updated"    Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="130" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Source"     Binding="{Binding SourceRef}"  Width="100" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Events Grid -->
                <DataGrid x:Name="EventsGrid"
                          ItemsSource="{Binding Events}"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          Visibility="Collapsed"
                          Style="{StaticResource MemoryDataGridStyle}"
                          CellStyle="{StaticResource MemoryDataGridCellStyle}"
                          ColumnHeaderStyle="{StaticResource MemoryColumnHeaderStyle}"
                          RowStyle="{StaticResource MemoryDataGridRowStyle}"
                          CellEditEnding="DataGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Type"    Binding="{Binding Type}"    Width="100" />
                        <DataGridTextColumn Header="Title"   Binding="{Binding Title}"   Width="*" />
                        <DataGridTextColumn Header="Summary" Binding="{Binding Summary}" Width="1.5*" />
                        <DataGridTextColumn Header="When"    Binding="{Binding WhenIso, StringFormat=yyyy-MM-dd HH:mm}" Width="130" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Source"  Binding="{Binding SourceRef}" Width="100" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Chunks Grid -->
                <DataGrid x:Name="ChunksGrid"
                          ItemsSource="{Binding Chunks}"
                          SelectedItem="{Binding SelectedItem, Mode=TwoWay}"
                          Visibility="Collapsed"
                          Style="{StaticResource MemoryDataGridStyle}"
                          CellStyle="{StaticResource MemoryDataGridCellStyle}"
                          ColumnHeaderStyle="{StaticResource MemoryColumnHeaderStyle}"
                          RowStyle="{StaticResource MemoryDataGridRowStyle}"
                          CellEditEnding="DataGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Source Type" Binding="{Binding SourceType}" Width="100" />
                        <DataGridTextColumn Header="Text"        Binding="{Binding Text}"       Width="*" />
                        <DataGridTextColumn Header="When"        Binding="{Binding WhenIso, StringFormat=yyyy-MM-dd HH:mm}" Width="130" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Source"      Binding="{Binding SourceRef}"   Width="100" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Empty state -->
                <StackPanel x:Name="MemoryEmptyState"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed">
                    <TextBlock Text="No memories stored yet"
                               FontSize="16"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Subtext0Brush}"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,6"/>
                    <TextBlock Text="Ask me to &quot;remember&quot; something in chat, or add one manually below."
                               FontSize="12"
                               Foreground="{StaticResource Overlay0Brush}"
                               HorizontalAlignment="Center"
                               TextWrapping="Wrap"
                               TextAlignment="Center"
                               MaxWidth="360"/>
                </StackPanel>
            </Grid>

            <!-- ── Footer: Pagination + Actions ──────────────────────── -->
            <Border Grid.Row="2"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,8"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Pagination -->
                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Button Content="&#x25C0; Prev"
                                Command="{Binding PrevPageCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding PageDisplay}"
                                   FontSize="11"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>
                        <Button Content="Next &#x25B6;"
                                Command="{Binding NextPageCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"/>
                    </StackPanel>

                    <!-- Status -->
                    <TextBlock Grid.Column="1"
                               Text="{Binding StatusText}"
                               FontSize="11"
                               Foreground="{StaticResource Overlay0Brush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <!-- Actions: constructive left, destructive right -->
                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Button Content="+ Add New"
                                Command="{Binding AddNewCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,6,0"/>
                        <Button Content="Refresh"
                                Command="{Binding RefreshCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,16,0"/>
                        <Border Width="1" Background="{StaticResource Surface1Brush}"
                                Margin="0,2"/>
                        <Button Content="Delete Selected"
                                Command="{Binding DeleteCommand}"
                                Style="{StaticResource MemoryDeleteButtonStyle}"
                                Margin="16,0,0,0"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             PROFILE / NUGGETS BROWSER
             User-initiated identity + personal facts management.
             DataContext is set to ProfileBrowserViewModel in code-behind.
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="ProfileView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>  <!-- Sub-tabs + search -->
                <RowDefinition Height="*"/>     <!-- Grid area -->
                <RowDefinition Height="Auto"/>  <!-- Footer -->
            </Grid.RowDefinitions>

            <!-- ── Sub-tabs + Nugget Search ──────────────────────────────── -->
            <Border Grid.Row="0"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,8"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,0,0,1">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <RadioButton Content="Profiles"
                                     IsChecked="True"
                                     GroupName="ProfileTabs"
                                     Click="ProfileSubTab_Click"
                                     Tag="Profiles"
                                     Style="{StaticResource MemorySubTabStyle}"/>
                        <RadioButton Content="Nuggets"
                                     GroupName="ProfileTabs"
                                     Click="ProfileSubTab_Click"
                                     Tag="Nuggets"
                                     Style="{StaticResource MemorySubTabStyle}"
                                     Margin="4,0,0,0"/>
                    </StackPanel>

                    <!-- Search box (nuggets only) -->
                    <TextBox Grid.Column="1"
                             x:Name="NuggetSearchBox"
                             Text="{Binding NuggetSearchText, UpdateSourceTrigger=PropertyChanged}"
                             Style="{StaticResource ChatInputStyle}"
                             FontSize="12"
                             Padding="8,5"
                             Margin="16,0,0,0"
                             Tag="Search nuggets..."/>
                </Grid>
            </Border>

            <!-- ── Grid Area ─────────────────────────────────────────────── -->
            <Grid Grid.Row="1" Background="{StaticResource BaseBrush}">

                <!-- Profile Cards DataGrid -->
                <DataGrid x:Name="ProfilesGrid"
                          ItemsSource="{Binding Profiles}"
                          SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                          Style="{StaticResource MemoryDataGridStyle}"
                          CellStyle="{StaticResource MemoryDataGridCellStyle}"
                          ColumnHeaderStyle="{StaticResource MemoryColumnHeaderStyle}"
                          RowStyle="{StaticResource MemoryDataGridRowStyle}"
                          CellEditEnding="ProfileGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Kind"         Binding="{Binding Kind}"         Width="80" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Display Name" Binding="{Binding DisplayName}"  Width="*"/>
                        <DataGridTextColumn Header="Relationship" Binding="{Binding Relationship}" Width="120"/>
                        <DataGridTextColumn Header="Aliases"      Binding="{Binding Aliases}"      Width="*"/>
                        <DataGridTextColumn Header="Profile JSON" Binding="{Binding ProfileJson}"  Width="1.5*"/>
                        <DataGridTextColumn Header="Updated"      Binding="{Binding UpdatedAt, StringFormat=yyyy-MM-dd HH:mm}" Width="130" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>

                <!-- Nuggets DataGrid -->
                <DataGrid x:Name="NuggetsGrid"
                          ItemsSource="{Binding Nuggets}"
                          SelectedItem="{Binding SelectedNugget, Mode=TwoWay}"
                          Visibility="Collapsed"
                          Style="{StaticResource MemoryDataGridStyle}"
                          CellStyle="{StaticResource MemoryDataGridCellStyle}"
                          ColumnHeaderStyle="{StaticResource MemoryColumnHeaderStyle}"
                          RowStyle="{StaticResource MemoryDataGridRowStyle}"
                          CellEditEnding="NuggetGrid_CellEditEnding">
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="Text"         Binding="{Binding Text}"        Width="*"/>
                        <DataGridTextColumn Header="Tags"         Binding="{Binding Tags}"        Width="140"/>
                        <DataGridTextColumn Header="Weight"       Binding="{Binding Weight, StringFormat=F2}" Width="70"/>
                        <DataGridTextColumn Header="Pin"          Binding="{Binding PinLevel}"    Width="50"/>
                        <DataGridTextColumn Header="Sensitivity"  Binding="{Binding Sensitivity}" Width="90"/>
                        <DataGridTextColumn Header="Uses"         Binding="{Binding UseCount}"    Width="50" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Last Used"    Binding="{Binding LastUsedAt, StringFormat=yyyy-MM-dd}" Width="100" IsReadOnly="True"/>
                        <DataGridTextColumn Header="Created"      Binding="{Binding CreatedAt, StringFormat=yyyy-MM-dd}" Width="100" IsReadOnly="True"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Grid>

            <!-- ── Footer: Actions ───────────────────────────────────────── -->
            <Border Grid.Row="2"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,8"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="Auto"/>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <!-- Pagination (nuggets only) -->
                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center"
                                x:Name="NuggetPaginationPanel"
                                Visibility="Collapsed">
                        <Button Content="◀ Prev"
                                Command="{Binding NuggetPrevPageCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,6,0"/>
                        <TextBlock Text="{Binding NuggetPageDisplay}"
                                   FontSize="11"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"
                                   Margin="0,0,6,0"/>
                        <Button Content="Next ▶"
                                Command="{Binding NuggetNextPageCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"/>
                    </StackPanel>

                    <!-- Status -->
                    <TextBlock Grid.Column="1"
                               Text="{Binding StatusText}"
                               FontSize="11"
                               Foreground="{StaticResource Overlay0Brush}"
                               HorizontalAlignment="Center"
                               VerticalAlignment="Center"/>

                    <!-- Actions: constructive left, destructive right -->
                    <StackPanel Grid.Column="2"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <Button x:Name="ProfileAddButton"
                                Content="+ Add Profile"
                                Command="{Binding AddProfileCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,6,0"/>
                        <Button x:Name="NuggetAddButton"
                                Content="+ Add Nugget"
                                Command="{Binding AddNuggetCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Visibility="Collapsed"
                                Margin="0,0,6,0"/>
                        <Button Content="Refresh"
                                Command="{Binding RefreshCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,16,0"/>
                        <Border Width="1" Background="{StaticResource Surface1Brush}"
                                Margin="0,2"/>
                        <Button x:Name="ProfileDeleteButton"
                                Content="Delete Selected"
                                Command="{Binding DeleteProfileCommand}"
                                Style="{StaticResource MemoryDeleteButtonStyle}"
                                Margin="16,0,0,0"/>
                        <Button x:Name="NuggetDeleteButton"
                                Content="Delete Selected"
                                Command="{Binding DeleteNuggetCommand}"
                                Style="{StaticResource MemoryDeleteButtonStyle}"
                                Margin="16,0,0,0"
                                Visibility="Collapsed"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             BRIEFING TAB
             Deep-Dive and research briefing results
             ═══════════════════════════════════════════════════════ -->
        <views:DeepDiveBriefingView Grid.Row="1" x:Name="BriefingView" Visibility="Collapsed" DataContext="{Binding BriefingPanel}"/>

        <!-- ═══════════════════════════════════════════════════════════
             TRANSCRIBE SUBTAB
             YouTube transcription controls
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="TranscribeSettingsView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Background="{StaticResource BaseBrush}">
                <StackPanel Margin="32,24,32,24" MaxWidth="560">

                    <TextBlock Text="YOUTUBE TRANSCRIPTION"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource MauveBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Download, transcribe, and summarize a YouTube video using the voice backend's ASR pipeline."
                               FontSize="10.5"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,16"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Video URL"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding YouTubeUrl, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ASR Provider"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding YouTubeAsrProvider, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="ASR Model"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding YouTubeAsrModelId, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Language Hint"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding YouTubeLanguageHint, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Draft Tone"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding AvailableDraftTones}"
                                  SelectedItem="{Binding YouTubeDraftTone, Mode=TwoWay}"
                                  Style="{StaticResource DarkComboBoxStyle}"
                                  ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"/>
                    </Grid>

                    <CheckBox Content="Keep converted audio files (do not clean work folder)"
                              IsChecked="{Binding YouTubeKeepAudio}"
                              Foreground="{StaticResource TextBrush}"
                              FontSize="12"
                              Margin="0,0,0,12"/>

                    <Border Height="1" Background="{StaticResource Surface0Brush}" Margin="0,0,0,16"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Button Content="Start YouTube Job"
                                Command="{Binding StartYouTubeJobCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,8,0"/>
                        <Button Content="Cancel"
                                Command="{Binding CancelYouTubeJobCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"/>
                    </StackPanel>

                    <TextBlock Text="{Binding YouTubeJobStatus}"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Subtext0Brush}"/>
                    <TextBlock Text="{Binding YouTubeJobStage}"
                               FontSize="11"
                               Foreground="{StaticResource Overlay0Brush}"
                               Margin="0,2,0,6"/>

                    <ProgressBar Height="8"
                                 IsIndeterminate="{Binding YouTubeJobIsRunning, Mode=OneWay}"
                                 Value="{Binding YouTubeJobProgressPercent, Mode=OneWay}"
                                 Maximum="100"
                                 Margin="0,0,0,6"/>
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <TextBlock Text="Progress: " FontSize="11" Foreground="{StaticResource Overlay0Brush}"/>
                        <TextBlock Text="{Binding YouTubeJobProgressPercent}" FontSize="11" Foreground="{StaticResource Overlay0Brush}"/>
                        <TextBlock Text="%" FontSize="11" Foreground="{StaticResource Overlay0Brush}"/>
                    </StackPanel>

                    <TextBlock Text="{Binding YouTubeErrorMessage}"
                               Foreground="{StaticResource RedBrush}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Transcript" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding YouTubeTranscriptPath}" FontSize="11" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Output Folder" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="1" Text="{Binding YouTubeOutputDir}" FontSize="11" Foreground="{StaticResource TextBrush}" TextWrapping="Wrap"/>
                    </Grid>

                    <TextBlock Text="Summary"
                               FontSize="12"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource Subtext0Brush}"
                               Margin="0,4,0,4"/>
                    <TextBox Text="{Binding YouTubeSummary, Mode=OneWay}"
                             IsReadOnly="True"
                             MinHeight="140"
                             MaxHeight="260"
                             VerticalScrollBarVisibility="Auto"
                             TextWrapping="Wrap"
                             Style="{StaticResource ChatInputStyle}"
                             FontSize="12"
                             Padding="8,6"/>

                </StackPanel>
            </ScrollViewer>

            <!-- Save bar -->
            <Border Grid.Row="1"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,10"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="●" FontSize="10" Foreground="{StaticResource YellowBrush}" Margin="0,0,6,0"
                                   Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{StaticResource Subtext0Brush}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Save Settings" Command="{Binding SaveCommand}" Style="{StaticResource SendButtonStyle}" Padding="14,8"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             LOGS TAB
             Full audit log feed and system activity
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="LogsView" Visibility="Collapsed" Background="{StaticResource BaseBrush}">
            <ScrollViewer x:Name="FullLogScroller"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                                <ItemsControl ItemsSource="{Binding DataContext.ActivityLog, RelativeSource={RelativeSource AncestorType=Window}}" Padding="32,24">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Grid Margin="0,6,0,10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding TimeDisplay}"
                                           FontFamily="{StaticResource CodeFont}"
                                           FontSize="12"
                                           Foreground="{StaticResource Surface2Brush}"
                                           Margin="0,2,16,0"
                                           VerticalAlignment="Top"/>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding Text}"
                                           TextWrapping="Wrap"
                                           FontFamily="{StaticResource PrimaryFont}"
                                           FontSize="13"
                                           Foreground="{StaticResource TextBrush}"/>
                            </Grid>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </ScrollViewer>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             SETTINGS
             Common configuration surfaced in-app. Changes are
             persisted to settings.json on save.
             ═══════════════════════════════════════════════════════ -->
        <!-- Settings Wrapper with Sub-tabs -->
        <Grid Grid.Row="1" x:Name="SettingsView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Border Grid.Row="0"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,8"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,0,0,1">
                <StackPanel Orientation="Horizontal" VerticalAlignment="Center">
                    <RadioButton x:Name="SettingsSubGeneral" Content="General" IsChecked="True" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="General" Style="{StaticResource MemorySubTabStyle}"/>
                    <RadioButton x:Name="SettingsSubPreferences" Content="Preferences" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="Preferences" Style="{StaticResource MemorySubTabStyle}" Margin="4,0,0,0"/>
                    <RadioButton x:Name="SettingsSubMcp" Content="MCP" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="MCP" Style="{StaticResource MemorySubTabStyle}" Margin="4,0,0,0"/>
                    <RadioButton x:Name="SettingsSubVoice" Content="Voice" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="Voice" Style="{StaticResource MemorySubTabStyle}" Margin="4,0,0,0"/>
                    <RadioButton x:Name="SettingsSubMemory" Content="Memory" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="Memory" Style="{StaticResource MemorySubTabStyle}" Margin="4,0,0,0"/>
                    <RadioButton x:Name="SettingsSubProfile" Content="Profile" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="Profile" Style="{StaticResource MemorySubTabStyle}" Margin="4,0,0,0"/>
                    <RadioButton x:Name="SettingsSubTranscribe" Content="Transcribe" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="Transcribe" Style="{StaticResource MemorySubTabStyle}" Margin="4,0,0,0"/>
                    <RadioButton x:Name="SettingsSubLogs" Content="Logs" GroupName="SettingsTabs" Click="SettingsSubTab_Click" Tag="Logs" Style="{StaticResource MemorySubTabStyle}" Margin="4,0,0,0"/>
                </StackPanel>
            </Border>

            <Grid Grid.Row="1" x:Name="SettingsContentArea"/>
        </Grid>

        <!-- Original Settings Content (now internal) -->
        <Grid Grid.Row="1" x:Name="SettingsGeneralView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Background="{StaticResource BaseBrush}">
                <StackPanel Margin="32,24,32,24" MaxWidth="560">

                    <controls:PersonalitySettingsControl Margin="0,0,0,20"/>

                    <Border Height="1"
                            Background="{StaticResource Surface0Brush}"
                            Margin="0,0,0,20"/>

                    <!-- ── Active Profile ──────────────────────────────── -->
                    <TextBlock Text="ACTIVE PROFILE"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource MauveBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Who is using the assistant right now? The selected profile is injected automatically. Edit the Near Me location in the Preferences tab."
                               FontSize="11.5"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"/>
                    <ComboBox x:Name="ProfileCombo"
                              ItemsSource="{Binding AvailableProfiles}"
                              SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                              DisplayMemberPath="DisplayLabel"
                              Style="{StaticResource DarkComboBoxStyle}"
                              ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"
                              Margin="0,0,0,8"/>

                    <WrapPanel Margin="0,0,0,12">
                        <Button Content="+ Add Profile"
                                Command="{Binding AddActiveProfileCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,8,8"/>
                        <Button Content="Edit JSON"
                                Command="{Binding EditActiveProfileJsonCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,8,8"/>
                        <Button Content="Refresh"
                                Command="{Binding RefreshActiveProfilesCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,0,8"/>
                    </WrapPanel>

                    <!-- ── Separator ───────────────────────────────────── -->
                    <Border Height="1"
                            Background="{StaticResource Surface0Brush}"
                            Margin="0,0,0,20"/>

                    <!-- ── LLM ─────────────────────────────────────────── -->
                    <TextBlock Text="LLM CONNECTION"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource BlueBrush}"
                               Margin="0,0,0,8"/>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Base URL"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding LlmBaseUrl, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Model"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding LlmModel, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Max Tokens"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding LlmMaxTokens, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>

                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Temperature"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding LlmTemperature, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12" Padding="8,6"/>
                    </Grid>


                    <TextBlock Text="HOTKEYS"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource TextBrush}"
                               Margin="0,0,0,6"/>
                               
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="Auto" SharedSizeGroup="HotkeyLabels"/>
                            <ColumnDefinition Width="200"/>
                        </Grid.ColumnDefinitions>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>
                        
                        <TextBlock Grid.Row="0" Grid.Column="0" 
                                   Text="Push-to-Talk (Global):"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   Margin="0,0,8,4"/>
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 Text="{Binding PttChord, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="4,2" Margin="0,0,0,4"/>

                        <TextBlock Grid.Row="1" Grid.Column="0" 
                                   Text="Stop Listening (Global):"
                                   FontSize="12"
                                   VerticalAlignment="Center"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   Margin="0,0,8,4"/>
                        <TextBox Grid.Row="1" Grid.Column="1"
                                 Text="{Binding ShutupChord, UpdateSourceTrigger=PropertyChanged}"
                                 Padding="4,2" Margin="0,0,0,4"/>
                    </Grid>

                    <TextBlock Text="Killswitch / STOP ALL (Global): Ctrl+Alt+Esc"
                               FontSize="12"
                               Foreground="{StaticResource RedBrush}"
                               Margin="0,0,0,4"/>
                    <TextBlock Text="Command Palette (Global): Ctrl+Space"
                               FontSize="12"
                               Foreground="{StaticResource Subtext0Brush}"
                               Margin="0,0,0,20"/>

                    <!-- ── Advanced Section ─────────────────────────────── -->
                    <Expander Header="Advanced Settings" 
                              Foreground="{StaticResource TextBrush}" 
                              FontSize="13" 
                              FontWeight="SemiBold" 
                              Margin="0,0,0,20">
                        <StackPanel Margin="0,12,0,0">
                            <!-- ── Runtime Safety ───────────────────── -->
                            <Expander Header="Runtime Safety" Foreground="{StaticResource RedBrush}" Margin="0,0,0,12" FontWeight="SemiBold">
                                <StackPanel Margin="0,12,0,0">
                                    <CheckBox Content="Enable panic mode (block side-effect tools)"
                                              IsChecked="{Binding RuntimePanicMode}"
                                              Foreground="{StaticResource TextBrush}"
                                              FontSize="12"
                                              Margin="0,0,0,4"/>
                                    <Grid Margin="0,0,0,8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="140"/>
                                            <ColumnDefinition Width="*"/>
                                        </Grid.ColumnDefinitions>
                                        <TextBlock Text="Safe mode status"
                                                   FontSize="12"
                                                   Foreground="{StaticResource Subtext0Brush}"
                                                   VerticalAlignment="Center"/>
                                        <TextBlock Grid.Column="1"
                                                   Text="{Binding RuntimeSafeMode, StringFormat=Active: {0}}"
                                                   FontSize="12"
                                                   Foreground="{StaticResource TextBrush}"
                                                   VerticalAlignment="Center"/>
                                    </Grid>
                                    <TextBlock Text="{Binding RuntimeSafeModeReason}"
                                               FontSize="10.5"
                                               Foreground="{StaticResource Overlay0Brush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,8"/>
                                    <TextBlock Text="Safe mode is fail-closed and runtime-managed. Use diagnostics to investigate and clear root causes."
                                               FontSize="10.5"
                                               Foreground="{StaticResource Overlay0Brush}"
                                               TextWrapping="Wrap"
                                               Margin="0,0,0,8"/>
                                </StackPanel>
                            </Expander>

                            <!-- ── Memory ────────────────────────────── -->
                            <Expander Header="Memory" Foreground="{StaticResource GreenBrush}" Margin="0,0,0,12" FontWeight="SemiBold">
                                <StackPanel Margin="0,12,0,0">
                                    <CheckBox Content="Enable memory (read + write)"
                                              IsChecked="{Binding MemoryEnabled}"
                                              Foreground="{StaticResource TextBrush}"
                                              FontSize="12"
                                              Margin="0,0,0,4"/>
                                    <TextBlock Text="When off, all memory features are disabled — no retrieval, no storage, no prompts."
                                               FontSize="10.5"
                                               Foreground="{StaticResource Overlay0Brush}"
                                               TextWrapping="Wrap"
                                               Margin="18,0,0,6"/>
                                    <CheckBox Content="Use embeddings for semantic search"
                                              IsChecked="{Binding EmbeddingsEnabled}"
                                              Foreground="{StaticResource TextBrush}"
                                              FontSize="12"
                                              Margin="0,0,0,8"/>
                                </StackPanel>
                            </Expander>

                            <!-- Privacy, Preferences, MCP, Voice Config, YouTube moved to dedicated tabs -->
                        </StackPanel>
                    </Expander>

                </StackPanel>
            </ScrollViewer>

            <!-- ── Sticky Save Bar ───────────────────────────────── -->
            <Border Grid.Row="1"
                    Background="{StaticResource MantleBrush}"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,1,0,0"
                    Padding="32,10">
                <Grid MaxWidth="560" HorizontalAlignment="Left">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0"
                                Orientation="Horizontal"
                                VerticalAlignment="Center">
                        <TextBlock Text="●"
                                   FontSize="10"
                                   Foreground="{StaticResource YellowBrush}"
                                   Margin="0,0,6,0"
                                   Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock x:Name="GeneralStatusText"
                                   Text="{Binding StatusText}"
                                   FontSize="11"
                                   FontWeight="Normal">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Overlay0Brush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSaveSuccess}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource GreenBrush}"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         From="0" To="1" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         To="0" Duration="0:0:0.4"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1"
                                Orientation="Horizontal">
                        <Button Content="Refresh"
                                Command="{Binding RefreshCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,8,0"/>
                        <Button Content="Save Settings"
                                Command="{Binding SaveCommand}"
                                Style="{StaticResource SendButtonStyle}"
                                Padding="14,8"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             PREFERENCES SUBTAB
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="PreferencesSettingsView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Background="{StaticResource BaseBrush}">
                <StackPanel Margin="32,24,32,24" MaxWidth="560">

                    <!-- ── Weather & Location ────────────────────────── -->
                    <TextBlock Text="WEATHER &amp; LOCATION"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource BlueBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Configure time format, weather units, and your Near Me location for weather and local context."
                               FontSize="11"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Time Format"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1"
                                  SelectedValue="{Binding Use24HourTime, Mode=TwoWay}"
                                  SelectedValuePath="Tag"
                                  Style="{StaticResource DarkComboBoxStyle}"
                                  ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"
                                  ToolTip="Toggle between 12-hour and 24-hour time formats.">
                            <ComboBoxItem Tag="False">12-hour (1:30 PM)</ComboBoxItem>
                            <ComboBoxItem Tag="True">24-hour (13:30)</ComboBoxItem>
                        </ComboBox>
                    </Grid>

                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Weather Units"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1"
                                  SelectedValue="{Binding WeatherPreferredUnits, Mode=TwoWay}"
                                  SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}"
                                  ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"
                                  ToolTip="Unit system for weather and measurements.">
                            <ComboBoxItem Content="imperial"/>
                            <ComboBoxItem Content="metric"/>
                            <ComboBoxItem Content="auto"/>
                        </ComboBox>
                    </Grid>

                    <Border Height="1"
                            Background="{StaticResource Surface0Brush}"
                            Margin="0,0,0,16"/>

                    <!-- ── Near Me Location ──────────────────────────── -->
                    <TextBlock Text="NEAR ME LOCATION"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource MauveBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Profile-scoped location used for weather and local context. Set a city, state, or ZIP code."
                               FontSize="11"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"/>

                    <Grid Margin="0,0,0,20">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="Auto"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Near Me location"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <TextBox Grid.Column="1"
                                 Text="{Binding LocationLabel, UpdateSourceTrigger=LostFocus}"
                                 Style="{StaticResource ChatInputStyle}"
                                 FontSize="12"
                                 Padding="8,6"
                                 Margin="8,0,8,0"
                                 ToolTip="City, State or ZIP for this profile"/>
                        <Button Grid.Column="2"
                                Content="Clear"
                                Click="ClearManualLocation_Click"
                                Style="{StaticResource MemoryActionButtonStyle}"/>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <!-- Save bar -->
            <Border Grid.Row="1"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,10"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="●" FontSize="10" Foreground="{StaticResource YellowBrush}" Margin="0,0,6,0"
                                   Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{StaticResource Subtext0Brush}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Save Settings" Command="{Binding SaveCommand}" Style="{StaticResource SendButtonStyle}" Padding="14,8"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             MCP PERMISSIONS SUBTAB
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="McpSettingsView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Background="{StaticResource BaseBrush}">
                <StackPanel Margin="32,24,32,24" MaxWidth="560">

                    <TextBlock Text="MCP PERMISSIONS"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource YellowBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Controls which tool groups the assistant may use. Off = hard block (no prompt). Ask = prompt every call. Always = auto-approve."
                               FontSize="10.5"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,16"/>

                    <!-- Developer Override -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Developer override"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="{StaticResource PeachBrush}"
                                   VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1"
                                  SelectedValue="{Binding McpPermDeveloperOverride, Mode=TwoWay}"
                                  SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}"
                                  ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                            <ComboBoxItem Content="none"/>
                            <ComboBoxItem Content="off"/>
                            <ComboBoxItem Content="ask"/>
                            <ComboBoxItem Content="always"/>
                        </ComboBox>
                    </Grid>
                    <TextBlock Text="Overrides Screen/Files/System/Web at runtime when not 'none'. Per-group settings are preserved and apply when override is reverted."
                               FontSize="10.5"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,16"/>

                    <Border Height="1" Background="{StaticResource Surface0Brush}" Margin="0,0,0,16"/>

                    <!-- Per-group dropdowns -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Screen" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding McpPermScreen, Mode=TwoWay}" SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}" ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                            <ComboBoxItem Content="off"/><ComboBoxItem Content="ask"/><ComboBoxItem Content="always"/>
                        </ComboBox>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Files" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding McpPermFiles, Mode=TwoWay}" SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}" ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                            <ComboBoxItem Content="off"/><ComboBoxItem Content="ask"/><ComboBoxItem Content="always"/>
                        </ComboBox>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="System" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding McpPermSystem, Mode=TwoWay}" SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}" ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                            <ComboBoxItem Content="off"/><ComboBoxItem Content="ask"/><ComboBoxItem Content="always"/>
                        </ComboBox>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Web" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding McpPermWeb, Mode=TwoWay}" SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}" ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                            <ComboBoxItem Content="off"/><ComboBoxItem Content="ask"/><ComboBoxItem Content="always"/>
                        </ComboBox>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Memory Read" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding McpPermMemoryRead, Mode=TwoWay}" SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}" ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                            <ComboBoxItem Content="off"/><ComboBoxItem Content="ask"/><ComboBoxItem Content="always"/>
                        </ComboBox>
                    </Grid>
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="140"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Memory Write" FontSize="12" Foreground="{StaticResource Subtext0Brush}" VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1" SelectedValue="{Binding McpPermMemoryWrite, Mode=TwoWay}" SelectedValuePath="Content"
                                  Style="{StaticResource DarkComboBoxStyle}" ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}">
                            <ComboBoxItem Content="off"/><ComboBoxItem Content="ask"/><ComboBoxItem Content="always"/>
                        </ComboBox>
                    </Grid>

                </StackPanel>
            </ScrollViewer>

            <!-- Save bar -->
            <Border Grid.Row="1"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,10"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="●" FontSize="10" Foreground="{StaticResource YellowBrush}" Margin="0,0,6,0"
                                   Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding StatusText}" FontSize="12" Foreground="{StaticResource Subtext0Brush}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Save Settings" Command="{Binding SaveCommand}" Style="{StaticResource SendButtonStyle}" Padding="14,8"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             VOICE SETTINGS SUBTAB
             ═══════════════════════════════════════════════════════ -->
        <Grid Grid.Row="1" x:Name="VoiceSettingsView" Visibility="Collapsed">
            <Grid.RowDefinitions>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <ScrollViewer Grid.Row="0"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled"
                          Background="{StaticResource BaseBrush}">
                <StackPanel Margin="32,24,32,24" MaxWidth="560">

                    <!-- ── Audio Devices ─────────────────────────────── -->
                    <TextBlock Text="AUDIO DEVICES"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource TealBrush}"
                               Margin="0,0,0,6"/>

                    <TextBlock Text="Select recording and playback devices. Use Test to verify levels before a voice session."
                               FontSize="10.5"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"/>

                    <!-- Input Device -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Input Device"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding AvailableInputDevices}"
                                  SelectedItem="{Binding SelectedInputDevice, Mode=TwoWay}"
                                  DisplayMemberPath="DisplayName"
                                  Style="{StaticResource DarkComboBoxStyle}"
                                  ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"/>
                    </Grid>

                    <!-- Output Device -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Output Device"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <ComboBox Grid.Column="1"
                                  ItemsSource="{Binding AvailableOutputDevices}"
                                  SelectedItem="{Binding SelectedOutputDevice, Mode=TwoWay}"
                                  DisplayMemberPath="DisplayName"
                                  Style="{StaticResource DarkComboBoxStyle}"
                                  ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"/>
                    </Grid>

                    <!-- Input Gain -->
                    <Grid Margin="0,0,0,12">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                            <ColumnDefinition Width="40"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Input Gain"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <Slider Grid.Column="1"
                                Value="{Binding InputGain}"
                                Minimum="0" Maximum="2"
                                TickFrequency="0.1"
                                IsSnapToTickEnabled="True"
                                VerticalAlignment="Center"/>
                        <TextBlock Grid.Column="2"
                                   Text="{Binding InputGain, StringFormat={}{0:F1}x}"
                                   FontSize="11"
                                   Foreground="{StaticResource Overlay0Brush}"
                                   VerticalAlignment="Center"
                                   HorizontalAlignment="Right"/>
                    </Grid>

                    <!-- Microphone Test -->
                    <TextBlock Text="MICROPHONE TEST"
                               FontSize="11" FontWeight="Bold"
                               Foreground="{StaticResource TealBrush}"
                               Margin="0,8,0,8"/>

                    <!-- Level Meter -->
                    <Grid Margin="0,0,0,8">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120"/>
                            <ColumnDefinition Width="*"/>
                        </Grid.ColumnDefinitions>
                        <TextBlock Text="Level"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                        <ProgressBar Grid.Column="1"
                                     Value="{Binding MicTestLevel, Mode=OneWay}"
                                     Minimum="0" Maximum="1"
                                     Height="10"
                                     Background="{StaticResource Surface0Brush}"
                                     Foreground="{StaticResource GreenBrush}"
                                     BorderThickness="0"/>
                    </Grid>

                    <!-- Test Status -->
                    <TextBlock Text="{Binding MicTestStatus}"
                               FontSize="10.5"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,8"/>

                    <!-- Test Buttons -->
                    <StackPanel Orientation="Horizontal" Margin="0,0,0,20">
                        <Button Content="Test Mic"
                                Command="{Binding TestMicCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,8,0"/>
                        <Button Content="Stop"
                                Command="{Binding StopTestMicCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,8,0"/>
                        <Button Content="Play Recording"
                                Command="{Binding PlayTestRecordingCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"
                                Margin="0,0,8,0"/>
                        <Button Content="Refresh Devices"
                                Command="{Binding RefreshDevicesCommand}"
                                Style="{StaticResource MemoryActionButtonStyle}"/>
                    </StackPanel>

                    <!-- ── Separator ───────────────────────────────────── -->
                    <Border Height="1"
                            Background="{StaticResource Surface0Brush}"
                            Margin="0,0,0,20"/>

                    <!-- TTS Engine Selection -->
                    <TextBlock Text="TTS Engine"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="Choose the text-to-speech engine. Piper is the recommended default — it's fast, local, and has no Python dependencies."
                               FontSize="11"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,16">
                        <RadioButton Content="Piper (recommended)"
                                     IsChecked="{Binding VoiceTtsEngine, Converter={StaticResource StringMatchConverter}, ConverterParameter=piper}"
                                     GroupName="TtsEngineGroup"
                                     Foreground="{StaticResource TextBrush}"
                                     Margin="0,0,16,0"/>
                        <RadioButton Content="Kokoro (advanced)"
                                     IsChecked="{Binding VoiceTtsEngine, Converter={StaticResource StringMatchConverter}, ConverterParameter=kokoro}"
                                     GroupName="TtsEngineGroup"
                                     Foreground="{StaticResource TextBrush}"
                                     Margin="0,0,16,0"/>
                        <RadioButton Content="Windows"
                                     IsChecked="{Binding VoiceTtsEngine, Converter={StaticResource StringMatchConverter}, ConverterParameter=windows}"
                                     GroupName="TtsEngineGroup"
                                     Foreground="{StaticResource TextBrush}"/>
                    </StackPanel>

                    <Border Height="1"
                            Background="{StaticResource Surface0Brush}"
                            Margin="0,0,0,16"/>

                    <!-- Voice Selection (engine-aware) -->
                    <TextBlock Text="{Binding VoiceSectionLabel}"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               Margin="0,0,0,8"/>
                    <TextBlock Text="{Binding VoiceSectionDescription}"
                               FontSize="11"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"/>

                    <!-- Piper voice dropdown (visible when engine=piper) -->
                    <ComboBox ItemsSource="{Binding AvailablePiperVoices}"
                              SelectedItem="{Binding SelectedPiperVoice, Mode=TwoWay}"
                              IsEditable="False"
                              Width="300"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,8"
                              Style="{StaticResource DarkComboBoxStyle}"
                              ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"
                              Visibility="{Binding IsPiperEngine, Converter={StaticResource BoolToVis}}"/>

                    <TextBlock Text="{Binding PiperVoiceDownloadStatus}"
                               FontSize="11"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"
                               Visibility="{Binding IsPiperStatusVisible, Converter={StaticResource BoolToVis}}">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Foreground" Value="{StaticResource GreenBrush}"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsPiperVoiceDownloading}" Value="True">
                                        <Setter Property="Foreground" Value="{StaticResource BlueBrush}"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>

                    <!-- Kokoro voice dropdown (visible when engine=kokoro) -->
                    <ComboBox ItemsSource="{Binding AvailableKokoroVoices}"
                              SelectedItem="{Binding SelectedKokoroVoice, Mode=TwoWay}"
                              IsEditable="False"
                              Width="300"
                              HorizontalAlignment="Left"
                              Margin="0,0,0,8"
                              Style="{StaticResource DarkComboBoxStyle}"
                              ItemContainerStyle="{StaticResource DarkComboBoxItemStyle}"
                              Visibility="{Binding IsKokoroEngine, Converter={StaticResource BoolToVis}}"/>

                    <!-- Windows engine note (visible when engine=windows) -->
                    <TextBlock Text="Using the built-in Windows speech synthesizer. No voice selection required."
                               FontSize="11"
                               FontStyle="Italic"
                               Foreground="{StaticResource Overlay0Brush}"
                               TextWrapping="Wrap"
                               Margin="0,0,0,12"
                               Visibility="{Binding IsWindowsEngine, Converter={StaticResource BoolToVis}}"/>

                    <Border Height="1"
                            Background="{StaticResource Surface0Brush}"
                            Margin="0,0,0,16"/>

                    <!-- Voice Host Status -->
                    <TextBlock Text="Voice Backend Status"
                               FontSize="13"
                               FontWeight="SemiBold"
                               Foreground="{StaticResource TextBrush}"
                               Margin="0,0,0,8"/>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,8">
                        <Ellipse Width="8" Height="8" VerticalAlignment="Center" Margin="0,0,6,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource Overlay0Brush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding VoiceHostIsRunning}" Value="True">
                                            <Setter Property="Fill" Value="{StaticResource GreenBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding VoiceHostMessage}"
                                   FontSize="12"
                                   Foreground="{StaticResource Subtext0Brush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,4">
                        <TextBlock Text="STT: "
                                   FontSize="11"
                                   Foreground="{StaticResource Overlay0Brush}"/>
                        <Ellipse Width="7" Height="7" VerticalAlignment="Center" Margin="0,0,4,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource Overlay0Brush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding VoiceHostAsrReady}" Value="True">
                                            <Setter Property="Fill" Value="{StaticResource GreenBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="faster-whisper"
                                   FontSize="11"
                                   Foreground="{StaticResource Overlay0Brush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                    <StackPanel Orientation="Horizontal" Margin="0,0,0,12">
                        <TextBlock Text="TTS: "
                                   FontSize="11"
                                   Foreground="{StaticResource Overlay0Brush}"/>
                        <Ellipse Width="7" Height="7" VerticalAlignment="Center" Margin="0,0,4,0">
                            <Ellipse.Style>
                                <Style TargetType="Ellipse">
                                    <Setter Property="Fill" Value="{StaticResource Overlay0Brush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding VoiceHostTtsReady}" Value="True">
                                            <Setter Property="Fill" Value="{StaticResource GreenBrush}"/>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </Ellipse.Style>
                        </Ellipse>
                        <TextBlock Text="{Binding VoiceTtsEngine}"
                                   FontSize="11"
                                   Foreground="{StaticResource Overlay0Brush}"
                                   VerticalAlignment="Center"/>
                    </StackPanel>

                </StackPanel>
            </ScrollViewer>

            <!-- Save bar (reuses same commands as General settings) -->
            <Border Grid.Row="1"
                    Background="{StaticResource MantleBrush}"
                    Padding="12,10"
                    BorderBrush="{StaticResource Surface0Brush}"
                    BorderThickness="0,1,0,0">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>

                    <StackPanel Grid.Column="0" Orientation="Horizontal" VerticalAlignment="Center">
                        <TextBlock Text="●"
                                   FontSize="10"
                                   Foreground="{StaticResource YellowBrush}"
                                   Margin="0,0,6,0"
                                   Visibility="{Binding HasUnsavedChanges, Converter={StaticResource BoolToVis}}"/>
                        <TextBlock Text="{Binding StatusText}"
                                   FontSize="12"
                                   VerticalAlignment="Center">
                            <TextBlock.Style>
                                <Style TargetType="TextBlock">
                                    <Setter Property="Foreground" Value="{StaticResource Subtext0Brush}"/>
                                    <Style.Triggers>
                                        <DataTrigger Binding="{Binding IsSaveSuccess}" Value="True">
                                            <Setter Property="Foreground" Value="{StaticResource GreenBrush}"/>
                                            <Setter Property="FontWeight" Value="SemiBold"/>
                                            <DataTrigger.EnterActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         From="0" To="1" Duration="0:0:0.2"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.EnterActions>
                                            <DataTrigger.ExitActions>
                                                <BeginStoryboard>
                                                    <Storyboard>
                                                        <DoubleAnimation Storyboard.TargetProperty="Opacity"
                                                                         To="0" Duration="0:0:0.4"/>
                                                    </Storyboard>
                                                </BeginStoryboard>
                                            </DataTrigger.ExitActions>
                                        </DataTrigger>
                                    </Style.Triggers>
                                </Style>
                            </TextBlock.Style>
                        </TextBlock>
                    </StackPanel>

                    <StackPanel Grid.Column="1" Orientation="Horizontal">
                        <Button Content="Save Settings"
                                Command="{Binding SaveCommand}"
                                Style="{StaticResource SendButtonStyle}"
                                Padding="14,8"/>
                    </StackPanel>
                </Grid>
            </Border>
        </Grid>

        <!-- ═══════════════════════════════════════════════════════════
             INPUT AREA
             ═══════════════════════════════════════════════════════ -->
        <Border Grid.Row="2" x:Name="InputArea"
                Background="{StaticResource MantleBrush}"
                Padding="12"
                BorderBrush="{StaticResource Surface0Brush}"
                BorderThickness="0,1,0,0">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <!-- Context chips -->
                <ItemsControl Grid.Row="0"
                              Grid.ColumnSpan="3"
                              ItemsSource="{Binding ContextChips}"
                              Margin="0,0,0,8">
                    <ItemsControl.ItemsPanel>
                        <ItemsPanelTemplate>
                            <WrapPanel Orientation="Horizontal"/>
                        </ItemsPanelTemplate>
                    </ItemsControl.ItemsPanel>
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <Border Margin="0,0,8,6"
                                    Padding="8,4"
                                    CornerRadius="10"
                                    BorderThickness="1">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="{StaticResource Surface0Brush}"/>
                                        <Setter Property="BorderBrush" Value="{StaticResource Surface1Brush}"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding IsWarning}" Value="True">
                                                <Setter Property="Background" Value="#44F38BA8"/>
                                                <Setter Property="BorderBrush" Value="#88F38BA8"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock Text="{Binding Text}"
                                           FontSize="11"
                                           Foreground="{StaticResource TextBrush}"/>
                            </Border>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>

                <!-- Text input -->
                <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,8,0">
                    <TextBox x:Name="ChatInput"
                             Style="{StaticResource ChatInputStyle}"
                             Text="{Binding InputText, UpdateSourceTrigger=PropertyChanged}"
                             TextWrapping="Wrap"
                             AcceptsReturn="False"
                             MaxHeight="120"
                             VerticalScrollBarVisibility="Auto"
                             PreviewKeyDown="ChatInput_PreviewKeyDown"/>
                    <!-- Placeholder watermark -->
                    <TextBlock Text="Type a message...  (Shift+Enter for new line)"
                               FontSize="15"
                               FontFamily="{StaticResource PrimaryFont}"
                               Foreground="{StaticResource Overlay0Brush}"
                               VerticalAlignment="Center"
                               Padding="16,12"
                               IsHitTestVisible="False">
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Visibility" Value="Collapsed"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding InputText}" Value="">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                    <DataTrigger Binding="{Binding InputText}" Value="{x:Null}">
                                        <Setter Property="Visibility" Value="Visible"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Grid>

                <!-- Voice button: Hold-to-Talk (normal) / Interrupt (when AI is processing) -->
                <Button x:Name="PttButton"
                        Grid.Row="1" Grid.Column="1"
                        IsEnabled="{Binding IsVoiceReady}"
                        PreviewMouseLeftButtonDown="PttButton_MouseDown"
                        PreviewMouseLeftButtonUp="PttButton_MouseUp"
                        MouseLeave="PttButton_MouseLeave"
                        ToolTip="Hold to record. Release to send."
                        Cursor="Hand"
                        Margin="0,0,8,0">
                    <Button.Template>
                        <ControlTemplate TargetType="Button">
                            <Border x:Name="HoldBg"
                                    Background="{StaticResource Surface1Brush}"
                                    BorderBrush="{StaticResource Surface2Brush}"
                                    BorderThickness="1"
                                    CornerRadius="4"
                                    Padding="12,6">
                                <StackPanel Orientation="Horizontal">
                                    <TextBlock x:Name="MicIcon" Text="&#xE720;"
                                               FontFamily="Segoe MDL2 Assets" FontSize="14"
                                               Foreground="{StaticResource Subtext1Brush}"
                                               VerticalAlignment="Center" Margin="0,0,6,0"/>
                                    <TextBlock x:Name="HoldLabel" Text="Hold" FontSize="12" FontWeight="SemiBold"
                                               Foreground="{StaticResource TextBrush}" VerticalAlignment="Center"/>
                                </StackPanel>
                            </Border>
                            <ControlTemplate.Triggers>
                                <!-- Hover (normal) -->
                                <Trigger Property="IsMouseOver" Value="True">
                                    <Setter TargetName="HoldBg" Property="Background" Value="{StaticResource Surface2Brush}"/>
                                    <Setter TargetName="HoldBg" Property="BorderBrush" Value="{StaticResource Overlay0Brush}"/>
                                </Trigger>
                                <!-- Pressed (recording) -->
                                <Trigger Property="IsPressed" Value="True">
                                    <Setter TargetName="HoldBg" Property="Background" Value="{StaticResource BlueBrush}"/>
                                    <Setter TargetName="HoldBg" Property="BorderBrush" Value="{StaticResource BlueBrush}"/>
                                    <Setter TargetName="HoldLabel" Property="Foreground" Value="{StaticResource CrustBrush}"/>
                                    <Setter TargetName="MicIcon" Property="Foreground" Value="{StaticResource CrustBrush}"/>
                                </Trigger>
                                <!-- Disabled -->
                                <Trigger Property="IsEnabled" Value="False">
                                    <Setter TargetName="HoldBg" Property="Opacity" Value="0.4"/>
                                </Trigger>
                                <!-- Processing state: AI is thinking (unclickable) -->
                                <DataTrigger Binding="{Binding IsProcessing}" Value="True">
                                    <Setter TargetName="MicIcon" Property="Text" Value="&#xE946;"/>
                                    <Setter TargetName="MicIcon" Property="Foreground" Value="{StaticResource Overlay0Brush}"/>
                                    <Setter TargetName="HoldLabel" Property="Text" Value="Processing…"/>
                                    <Setter TargetName="HoldLabel" Property="Foreground" Value="{StaticResource Overlay0Brush}"/>
                                    <Setter TargetName="HoldBg" Property="BorderBrush" Value="{StaticResource Surface1Brush}"/>
                                    <Setter TargetName="HoldBg" Property="Background" Value="{StaticResource Surface0Brush}"/>
                                    <Setter TargetName="HoldBg" Property="IsHitTestVisible" Value="False"/>
                                </DataTrigger>
                                <!-- Processing state: voice input path (Thinking state) -->
                                <DataTrigger Binding="{Binding DataContext.StateLabel, ElementName=HeaderArea}" Value="Thinking">
                                    <Setter TargetName="MicIcon" Property="Text" Value="&#xE946;"/>
                                    <Setter TargetName="MicIcon" Property="Foreground" Value="{StaticResource Overlay0Brush}"/>
                                    <Setter TargetName="HoldLabel" Property="Text" Value="Processing…"/>
                                    <Setter TargetName="HoldLabel" Property="Foreground" Value="{StaticResource Overlay0Brush}"/>
                                    <Setter TargetName="HoldBg" Property="BorderBrush" Value="{StaticResource Surface1Brush}"/>
                                    <Setter TargetName="HoldBg" Property="Background" Value="{StaticResource Surface0Brush}"/>
                                    <Setter TargetName="HoldBg" Property="IsHitTestVisible" Value="False"/>
                                </DataTrigger>
                                <!-- Interrupt mode: AI is speaking (TTS playback) -->
                                <DataTrigger Binding="{Binding DataContext.StateLabel, ElementName=HeaderArea}" Value="Speaking">
                                    <Setter TargetName="MicIcon" Property="Text" Value="&#xE71A;"/>
                                    <Setter TargetName="MicIcon" Property="Foreground" Value="{StaticResource RedBrush}"/>
                                    <Setter TargetName="HoldLabel" Property="Text" Value="Interrupt"/>
                                    <Setter TargetName="HoldLabel" Property="Foreground" Value="{StaticResource RedBrush}"/>
                                    <Setter TargetName="HoldBg" Property="BorderBrush" Value="{StaticResource RedBrush}"/>
                                    <Setter TargetName="HoldBg" Property="Background" Value="Transparent"/>
                                </DataTrigger>
                            </ControlTemplate.Triggers>
                        </ControlTemplate>
                    </Button.Template>
                </Button>

                <!-- Send / Cancel button swap -->
                <Grid Grid.Row="1" Grid.Column="2">
                    <!-- Send (visible when NOT processing) -->
                    <Button Content="Send ↵"
                            Command="{Binding SendCommand}">
                        <Button.Style>
                            <Style TargetType="Button"
                                   BasedOn="{StaticResource SendButtonStyle}">
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsProcessing}"
                                                 Value="True">
                                        <Setter Property="Visibility"
                                                Value="Collapsed"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </Button.Style>
                    </Button>

                    <!-- Cancel (visible when processing) -->
                    <Button Content="Cancel"
                            Command="{Binding CancelCommand}"
                            Visibility="{Binding IsProcessing,
                                         Converter={StaticResource BoolToVis}}"
                            Style="{StaticResource CancelButtonStyle}"/>
                </Grid>
            </Grid>
        </Border>

        <!-- ═══════════════════════════════════════════════════════════
             ACTIVITY DRAWER (Overlay)
             ═══════════════════════════════════════════════════════ -->
        <!-- Light-dismiss overlay (click outside to close drawer) -->
        <Border Grid.Row="1" Grid.RowSpan="2"
                Background="Transparent"
                DataContext="{Binding ElementName=HeaderArea, Path=DataContext}"
                Visibility="{Binding IsDrawerOpen, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="99"
                MouseLeftButtonDown="DrawerDismiss_Click"/>

        <Border Grid.Row="1" Grid.RowSpan="2"
                HorizontalAlignment="Right"
                Width="320"
                Background="{StaticResource CrustBrush}"
                BorderBrush="{StaticResource Surface1Brush}"
                BorderThickness="1,0,0,0"
                DataContext="{Binding ElementName=HeaderArea, Path=DataContext}"
                Visibility="{Binding IsDrawerOpen, Converter={StaticResource BoolToVis}}"
                Panel.ZIndex="100">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>

                <!-- Drawer Header -->
                <Border Grid.Row="0"
                        Background="{StaticResource MantleBrush}"
                        Padding="16,12"
                        BorderBrush="{StaticResource Surface0Brush}"
                        BorderThickness="0,0,0,1">
                    <TextBlock Text="Activity Log"
                               FontSize="12"
                               FontWeight="SemiBold"
                               FontFamily="{StaticResource PrimaryFont}"
                               Foreground="{StaticResource Subtext0Brush}"/>
                </Border>

                <!-- Audit Feed -->
            <ScrollViewer Grid.Row="1"
                          x:Name="LogScroller"
                          VerticalScrollBarVisibility="Auto"
                          HorizontalScrollBarVisibility="Disabled">
                            <ItemsControl ItemsSource="{Binding DataContext.DrawerLog, RelativeSource={RelativeSource AncestorType=Window}}" Padding="12,8">
                    <ItemsControl.ItemTemplate>
                            <DataTemplate>
                            <Grid Margin="0,4">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0"
                                           Text="{Binding TimeDisplay}"
                                           FontFamily="{StaticResource CodeFont}"
                                           FontSize="11"
                                           Foreground="{StaticResource Surface2Brush}"
                                           Margin="0,0,8,0"
                                           VerticalAlignment="Top"/>

                                <TextBlock Grid.Column="1"
                                           Text="{Binding Text}"
                                           TextWrapping="Wrap"
                                           FontFamily="{StaticResource PrimaryFont}"
                                           FontSize="12"
                                           Foreground="{StaticResource TextBrush}"/>
                            </Grid>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </ScrollViewer>

                <!-- Empty state -->
                <TextBlock Grid.Row="1"
                           Text="No recent actions"
                           Foreground="{StaticResource Overlay0Brush}"
                           FontSize="12"
                           FontStyle="Italic"
                           Margin="0,20,0,0"
                           HorizontalAlignment="Center">
                    <TextBlock.Style>
                        <Style TargetType="TextBlock">
                            <Setter Property="Visibility" Value="Collapsed"/>
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding DataContext.DrawerLog.Count, RelativeSource={RelativeSource AncestorType=Window}}" Value="0">
                                    <Setter Property="Visibility" Value="Visible"/>
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </TextBlock.Style>
                </TextBlock>
            </Grid>
        </Border>
    </Grid>
</Window>
