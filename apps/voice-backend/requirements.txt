# Voice backend dependencies
# Install: pip install -r requirements.txt
# Keep deployable from the immutable offline wheelhouse in assets-v2.
fastapi>=0.139.0
uvicorn[standard]>=0.51.0
python-multipart>=0.0.32
# The bundled runtime is Python 3.11; NumPy 2.5+ requires Python 3.12.
numpy>=2.4.6,<2.5
faster-whisper>=1.2.1
# Ensures VC++ runtime DLLs are available on fresh Windows machines where
# ctranslate2/faster-whisper can otherwise crash the process during model load.
msvc-runtime>=14.44.35112; platform_system == "Windows"
yt-dlp
