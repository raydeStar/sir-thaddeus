<UserControl x:Class="SirThaddeus.DesktopRuntime.Views.SourcesFlyout"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d">
    <Expander Header="{Binding Count, StringFormat=Sources ({0})}"
              IsExpanded="False">
        <ItemsControl ItemsSource="{Binding}" Margin="0,6,0,0">
            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <StackPanel Margin="0,0,0,8">
                        <TextBlock Text="{Binding Name}"
                                   Foreground="{DynamicResource TextBrush}"
                                   FontWeight="SemiBold"
                                   FontSize="11" />
                        <TextBlock TextWrapping="Wrap" FontSize="11">
                            <Hyperlink NavigateUri="{Binding Url}"
                                       RequestNavigate="Hyperlink_RequestNavigate"
                                       Style="{DynamicResource AccentHyperlinkStyle}">
                                <Run Text="{Binding Url, Mode=OneWay}" />
                            </Hyperlink>
                        </TextBlock>
                        <TextBlock Text="{Binding FetchedIso, StringFormat=Fetched: {0}}"
                                   Foreground="{DynamicResource Overlay2Brush}"
                                   FontSize="10" />
                    </StackPanel>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </Expander>
</UserControl>
