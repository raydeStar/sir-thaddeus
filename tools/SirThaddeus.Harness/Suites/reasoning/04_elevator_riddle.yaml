id: reasoning_elevator_riddle
name: Reasoning - Elevator rainy day hidden assumption trap
user_message: "A man lives on the 20th floor of a building. Every day he takes the elevator down to the ground floor to go to work. When he returns, he takes the elevator to the 10th floor and walks the remaining 10 floors — except on rainy days, when he takes the elevator all the way to the 20th floor. Why?"
allowed_tools: []
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - web_search
    - browser_navigate
    - file_read
    - file_list
    - system_execute
    - screen_capture
    - weather_geocode
    - weather_forecast
    - places_lookup
    - tool_ping
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "short"
    - "umbrella"
    - "button"
  forbidden_keywords:
    - error
    - timeout
    - as an ai
    - i can't
    - i cannot
    - stamina
    - exercise
  max_response_chars: 2400
min_score: 8.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/Search/
  tier2_targets:
    - packages/agent/SirThaddeus.Agent/
  tier3_targets:
    - packages/
