# answers verified via web 2026-07-03; open-book lane — never compare to closed-book numbers
id: probe_open_debian_13_codename
name: Knowledge Probe (Open) - Debian 13 Codename
rubric_profile: general
user_message: "Use the web_search tool to find this. What is the one-word release codename of Debian 13, the stable release the Debian project published in August 2025? Reply with only the codename."
allowed_tools: [web_search]
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - python_eval
    - system_execute
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "trixie"
  forbidden_keywords:
    - as an ai
  max_response_chars: 400
min_score: 5.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/
  tier2_targets:
    - packages/
  tier3_targets:
    - packages/
