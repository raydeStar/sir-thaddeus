# answers verified via web 2026-07-03; open-book lane — never compare to closed-book numbers
id: probe_open_spherex_launch_year
name: Knowledge Probe (Open) - SPHEREx Launch Year
rubric_profile: general
user_message: "Use the web_search tool to find this. In what year did NASA launch its SPHEREx space telescope (the near-infrared all-sky survey mission that shared a Falcon 9 ride with the PUNCH satellites from Vandenberg)? Reply with only the year."
allowed_tools: [web_search]
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - python_eval
    - system_execute
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "2025"
  forbidden_keywords:
    - as an ai
  max_response_chars: 400
min_score: 5.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/
  tier2_targets:
    - packages/
  tier3_targets:
    - packages/
