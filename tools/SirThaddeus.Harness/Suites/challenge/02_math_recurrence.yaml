id: challenge_math_recurrence
name: Challenge - Recurrence Arithmetic
rubric_profile: general
user_message: "A sequence starts with a1 = 2 and a2 = 3. For n >= 3, a_n = a_{n-1} + 2a_{n-2}. What is a6? Show the sequence values you used and put the final answer on its own line."
allowed_tools: []
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - web_search
    - browser_navigate
    - file_read
    - system_execute
    - screen_capture
    - tool_ping
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "53"
  forbidden_keywords:
    - error
    - timeout
    - as an ai
  max_response_chars: 1000
min_score: 8.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/PostProcessing/
    - packages/agent/SirThaddeus.Agent/Routing/
  tier2_targets:
    - packages/agent/SirThaddeus.Agent/
  tier3_targets:
    - packages/
