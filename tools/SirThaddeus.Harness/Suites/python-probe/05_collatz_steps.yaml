id: probe_py_collatz_steps
name: Python Probe - Collatz Trajectory Length
rubric_profile: general
user_message: "Use the python_eval tool to compute this. Starting from 27, how many Collatz steps (n -> n/2 if even, n -> 3n+1 if odd) does it take to reach 1? Reply with only the integer."
allowed_tools: [python_eval]
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - web_search
    - system_execute
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "111"
  forbidden_keywords:
    - as an ai
  max_response_chars: 400
min_score: 5.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/
  tier2_targets:
    - packages/
  tier3_targets:
    - packages/
