id: probe_py_max_collatz
name: Python Probe - Longest Collatz Trajectory
rubric_profile: general
user_message: "Use the python_eval tool to compute this. Among all starting values from 1 through 100, what is the greatest number of Collatz steps (n -> n/2 if even, n -> 3n+1 if odd) needed to reach 1? Reply with only the integer."
allowed_tools: [python_eval]
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - web_search
    - system_execute
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "118"
  forbidden_keywords:
    - as an ai
  max_response_chars: 400
min_score: 5.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/
  tier2_targets:
    - packages/
  tier3_targets:
    - packages/
