id: probe_py_happy_numbers
name: Python Probe - Happy Numbers
rubric_profile: general
user_message: "Use the python_eval tool to compute this. A happy number reaches 1 when you repeatedly replace it with the sum of the squares of its digits. How many happy numbers are there below 500? Reply with only the integer."
allowed_tools: [python_eval]
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - web_search
    - system_execute
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "76"
  forbidden_keywords:
    - as an ai
  max_response_chars: 400
min_score: 5.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/
  tier2_targets:
    - packages/
  tier3_targets:
    - packages/
