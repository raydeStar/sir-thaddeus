id: probe_py_recurrence_b8
name: Python Probe - Non-homogeneous Recurrence
rubric_profile: general
user_message: "Use the python_eval tool to compute this. Let b1 = 3 and b_n = 2*b_{n-1} + n for n >= 2. What is b8? Reply with only the integer."
allowed_tools: [python_eval]
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - web_search
    - system_execute
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "758"
  forbidden_keywords:
    - as an ai
  max_response_chars: 400
min_score: 5.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/
  tier2_targets:
    - packages/
  tier3_targets:
    - packages/
