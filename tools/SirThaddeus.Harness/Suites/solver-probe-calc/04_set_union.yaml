id: probe_calc_set_union
name: Solver Probe (calculator) - Inclusion-Exclusion
rubric_profile: general
user_message: "Use the calculator tool to compute the arithmetic. In a group, 22 people know Python, 19 know JavaScript, and 8 know both. How many know at least one of the two? Reply with only the integer."
allowed_tools: [calculator]
mode: live
assertions:
  required_tools: []
  forbidden_tools:
    - web_search
    - system_execute
  allowed_tools_only: true
  require_structured_errors: true
  require_no_hallucinated_citations: true
expectations:
  required_keywords:
    - "33"
  forbidden_keywords:
    - as an ai
  max_response_chars: 400
min_score: 5.0
patch_targets:
  tier1_targets:
    - packages/agent/SirThaddeus.Agent/
  tier2_targets:
    - packages/
  tier3_targets:
    - packages/
